apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: language-tutor-data
  namespace: language-tutor
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: local-path
